<%= form_for(@cert_type) do |f| %>
  <% if @cert_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cert_type.errors.count, "error") %> prohibited this cert_type from being saved:</h2>

      <ul>
      <% @cert_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div style="display: inline; float: left; width: 60%;">
    <fieldset><legend>Certificate type details</legend>

      <div> <!-- wrapper around account details and help divs -->
        <div class="field" style="float: left;">
    <%= f.label :name %><br>
    <%= f.text_field :name, :class => "serverlist" %>
  </div>
      </div>
    </fieldset>
	</div>
  <div style="clear: both;"></div>

  <div class="actions">
    <%= f.submit :class => "button" %>
  </div>
<% end %>
