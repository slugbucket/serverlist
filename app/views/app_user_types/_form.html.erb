<%= form_for(@app_user_type) do |f| %>
  <% if @app_user_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@app_user_type.errors.count, "error") %> prohibited this app_user_type from being saved:</h2>

      <ul>
      <% @app_user_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div style="display: inline; float: left; width: 60%;">
    <fieldset><legend>Application user type details</legend>

      <div> <!-- wrapper around account details and help divs -->
        <div class="field" style="float: left;">
    <%= f.label :name %><br />
    <%= f.text_field :name, :class => "serverlist" %>
	</div>
  <div style="clear: both;"></div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description, :rows => 6, :cols => 40, :class => "mceEditor" %>
  </div>
      </div>
    </fieldset>
  <div style="clear: both;"></div>

  <div class="actions">
    <%= f.submit :class => "button" %>
  </div>
<% end %>
