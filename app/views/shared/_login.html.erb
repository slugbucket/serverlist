	<% if current_session %>
    Logged in as <%= current_session.username %>. <%= link_to "Logout", destroy_session_session_path, :method => 'delete' %>
    <% else %>
    <%= link_to 'Login', new_session_session_path, :id => 'login-link' %>
    <% end %>