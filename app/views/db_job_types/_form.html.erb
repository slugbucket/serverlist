<%= form_for(@db_job_type) do |f| %>
  <% if @db_job_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@db_job_type.errors.count, "error") %> prohibited this db_job_type from being saved:</h2>

      <ul>
      <% @db_job_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div style="display: inline; float: left; width: 80%;">
    <fieldset><legend>Database job status details</legend>

      <div> <!-- wrapper around account details and help divs -->
  <div class="field" style="float: left;">
    <%= f.label :name %><br>
    <%= f.text_field :name, :class => "serverlist" %>
  </div>
    </fieldset>
  <div style="clear: both;"></div>

  <div class="actions">
    <%= f.submit "Save job status", :class => "button" %>
  </div>
<% end %>