<%= form_for(@package_architecture) do |f| %>
  <% if @package_architecture.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@package_architecture.errors.count, "error") %> prohibited this package_architecture from being saved:</h2>

      <ul>
      <% @package_architecture.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div style="display: inline; float: left; width: 80%;">
    <fieldset><legend>Package architecture details</legend>
      <div> <!-- wrapper around account details and help divs -->
       <div class="field" style="float: left;">
        <%= f.label :name %><br />
        <%= f.text_field :name, :class => "serverlist" %>
       </div>
       <div style="display: inline; float: left; width: 80%;">
        <div> <!-- wrapper around account details and help divs -->
         <div class="field" style="float: left;">
          <%= f.label :description %><br />
          <%= f.text_area :description, :class => "serverlist", :rows => 6, :cols => 80 %>
         </div>
        </div>
       </div>
    </fieldset>
  </div>
  <div style="clear: both;"></div>
  <div class="actions">
   <%= f.submit :class => "button" %>
  </div>
<% end %>
