<h2>Listing Database replication types</h2>
<p>
<div class="digg_pagination">  
<%= will_paginate @db_repl_types %>
<% if can? :create, DbReplType %>
<%= link_to 'New Database replication type', new_db_repl_type_path %>
<% end %>
</div>
</p>
<br />
<p style="clear: both">
<table class="serverlist" cellspacing="0">
  <thead>
    <tr class="serverlist">
      <th>Name</th>
      <th></th>
	  <th></th>
    </tr>
  </thead>

  <tbody>
    <% @db_repl_types.each do |db_repl_type| %>
    <tr class="<%= cycle('ma_row_light', 'ma_row_dark') %>">
    <%= render :partial => "shared/edit_link", :object => db_repl_type, :locals => {:display => db_repl_type.name, :edit_path => edit_db_repl_type_path(db_repl_type)} %>
        <%= render :partial => "shared/delete_link", :object => db_repl_type, :locals => {:display => "Delete #{db_repl_type.name}" } %>
        </tr>
    <% end %>
  </tbody>
</table>

<p>
<div class="digg_pagination">  
<%= will_paginate @db_repl_types %>
<% if can? :create, DbReplType %>
<%= link_to 'New Database replication type', new_db_repl_type_path %>
<% end %>
</div>
</p>
