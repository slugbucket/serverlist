<%= form_for(@oob_remote_mngmt) do |f| %>
  <% if @oob_remote_mngmt.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@oob_remote_mngmt.errors.count, "error") %> prohibited this oob_remote_mngmt from being saved:</h2>

      <ul>
      <% @oob_remote_mngmt.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div style="display: inline; float: left; width: 80%;">
    <fieldset><legend>Out of band management details</legend>

      <div> <!-- wrapper around account details and help divs -->
  <div class="field" style="float: left;">
    <%= f.label :name %><br />
    <%= f.text_field :name, :class => "serverlist" %>
  </div>
  <div style="clear: both;"></div>
  <div class="field">
      <%= f.label :description %><br>
      <%= f.text_area :description, :rows => 6, :cols => 40, :class => "mceEditor" %>
    </div>
    </fieldset>
    </div>
  <div style="clear: both;"></div>

  <div class="actions">
    <%= f.submit :class => "button" %>
  </div>
<% end %>
